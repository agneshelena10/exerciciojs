let Degraus = Number(prompt('Digite o tamanho da escada'));
let modeloMaterial = prompt('Digite o modelo do material');
let escadinha = '';


for(let a = 0; a < Degraus; a++ ){
    
    escadinha = escadinha + modeloMaterial ;
    console.log(escadinha);
}
